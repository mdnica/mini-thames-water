# Mini Thames Water Clone 💧

A full-stack web app inspired by Thames Water’s customer portal.  
Built with **React (frontend)** and **Node.js + SQLite (backend)**.

## Setup

1. Clone the repo
2. Run `npm install` in both `/server` and `/client`
3. Start backend → `node server.js`
4. Start frontend → `npm start` in `/client`

---

✅ Environment: Node.js v20.19.5  
✅ Database: SQLite  
✅ Auth: JWT + bcrypt
